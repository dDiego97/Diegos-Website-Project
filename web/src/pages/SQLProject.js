import React from "react";

function SQLProject() {
  return (
    <div style={{ padding: "2rem" }}>
      <h1>SQL Database Demo</h1>
      <p>
        This page will show SQL data fetched from a backend API. Later, this can
        connect to an ASP.NET Core Web API with SQL Server or Azure SQL.
      </p>
    </div>
  );
}

export default SQLProject;
